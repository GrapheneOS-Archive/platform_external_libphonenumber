apply plugin: 'java'

sourceSets.main {
    java.srcDirs = ['libphonenumber/src', 'geocoder/src', 'internal/prefixmapper/src']
    resources.srcDirs = ['libphonenumber/src']
}

java {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}
